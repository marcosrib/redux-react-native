const INITIAL_STATE = {
        adiciona_contato_email: 'teste@teste'
};

export default (state = INITIAL_STATE, action) => {
        switch(action.type) {
            default:
                return state;    
        }

}